<h1>New team</h1>

<%= render 'form' %>

<%= link_to 'Back', @team %>
<%#= form_for(@team, :url => stage_group_teams_path(params[:stage_id], params[:group_id], @team.id)) do |f| %>
<%#= render :partial => "teams/form", :locals=>{:f=>f, :group=>@group}%>
<%# end %>

<%#= link_to 'Back', stage_group_path(params[:stage_id], params[:group_id]) %>