<h1>New bet for player #<%= @player.name %></h1>

<%= form_for(@bet, :url => player_bets_path(params[:player_id])) do |f| %>
<%= render :partial => "bets/form", :locals=>{:f=>f, :bet=>@bet, :match_editable=>true} %>
<% end %>
