<h1>Editing group</h1>

<%= form_for(@group, :url => stage_group_path(@group.stage_id, @group.id)) do |f| %>
<%= render :partial => "groups/form", :locals=>{:f=>f, :group=>@group} %>
<% end %>

<%= link_to 'Back', stage_group_path(@group.stage.id, @group.id) %>
